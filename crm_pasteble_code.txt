style={{
  padding:
    typeof window !== "undefined" &&
    window.innerWidth < 768
      ? 10
      : 20,

  background: "#f5f5f5",

  minHeight: "100vh",

  marginLeft:
    typeof window !== "undefined" &&
    window.innerWidth < 768
      ? 0
      : 230,

  overflowX: "hidden",
}}
